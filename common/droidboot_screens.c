#include <lvgl.h>
#include <droidboot_screens.h>

void screens_init()
{
    lv_obj_t *boot_logo = lv_obj_create(NULL);
    lv_obj_t *fastboot = lv_obj_create(NULL);
    lv_obj_t *setup_main = lv_obj_create(NULL);
    lv_obj_t *setup_about = lv_obj_create(NULL);
    lv_obj_t *dualboot_menu = lv_obj_create(NULL);
}
